{
  "name": "magnum-topo",
  "version": "0.1.1",
  "description": "Sorts magnum-loader plugins.",
  "main": "index.js",
  "scripts": {
    "test": "NODE_ENV=test tap -R spec --cov test/unit/*.js",
    "test:report": "NODE_ENV=test tap -R spec --cov --coverage-report=lcov test/unit/*.js",
    "test:file": "NODE_ENV=test tap -R spec"
  },
  "author": "Jim <monstertke@paperelectron.com> (http://paperelectron.com/)",
  "license": "MIT",
  "dependencies": {
    "lodash": "^4.13.1"
  },
  "devDependencies": {
    "tap": "^5.7.2"
  }
}
